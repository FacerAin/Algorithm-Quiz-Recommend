import React from 'react';
import RegisterContainer from '../containters/auth/RegisterContainer';

const RegisterPage = () => {
  return (
      <RegisterContainer type="register" />
  );
};

export default RegisterPage;